# Deployments' Configuration Files

!!!abstract
    This folder's hosts the  deployments' core **`configuration`** files. These files povide the project-specific parameters needed by the deployments' scripts. The  data it contains essentially concern the project's specific structure and, in particular, the way the deployment  environments map to the `deploy/` subfolders and where to find the project's releases and these of its subprojects, if any.
