# Libraries Assets

!!!Abstract
    This folder hosts the **`libraries`** assets, which  typically consist of collections of classe orfunctions meant to be invoked by other programs. Typical examples include  `frameworks`, `packages` and, of course, `libraries`.
